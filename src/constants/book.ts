export const DEFAULT_COVER_URI = `${window.location.origin}/book-default-cover.jpg`

export enum SORT_TYPES {
  Prev = 'prev',
  Next = 'next'
}

export enum SORT_BY {
  Title = 'title',
  Authors = 'authors',
  CreatedAt = 'createdAt'
}
